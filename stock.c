/* Name: stock.c
 * Description: functions for `stock_t`
*/
#include "stock.h"
#include <stdio.h>


void getStock( stock_t* stock ) {

}

// assumes `directory` is not NULL
void report(DIR* directory);

void buy(linked_list_t *list);

void sell();
